# Bronze Accord Recovery Kit

This archive contains all critical components of the Bronze Accord ethical framework and its supporting tools.

---

## ✅ Update: SAGE Module Patch Applied (2025-07-14)

The following files were patched and reinserted into the `SAGE/` folder of the archive:

- `semantic_conflict_detector.py` — Added `detect_conflict()` top-level wrapper
- `semantic_matcher.py` — Added `match_beliefs()` top-level wrapper
- `edge_case_reasoning.py` — Added `evaluate_edge_case()` top-level wrapper
- `text_similarity_utils.py` — Patched to handle `compute_similarity_score()` with bundles

Patch bundle also available separately as: `SAGE_Patch_v20250714.zip`

---
